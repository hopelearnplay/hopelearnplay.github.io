/* ============================================================
   TOKENS
============================================================ */
:root{
  --cream:#FFFBF2;
  --sand:#FFF3DC;
  --ink:#2E2140;
  --ink-soft:#5B4B72;
  --teal:#2AAFC4;
  --teal-dark:#1B8FA0;
  --purple:#8B2E93;
  --purple-dark:#6B1F72;
  --orange:#E08A34;
  --pink:#D14A8F;
  --wine:#9C2A63;
  --yellow:#FFD93D;
  --yellow-deep:#F5B921;
  --line:rgba(46,33,64,.12);
  --shadow:0 12px 30px rgba(46,33,64,.12);
  --radius:22px;
  --maxw:1180px;
  --header-h:76px;
}

html, body {
  overscroll-behavior-x: none;
  touch-action: pan-y;
}

*,*::before,*::after{box-sizing:border-box}
html{scroll-behavior:smooth; scroll-padding-top:calc(var(--header-h) + 14px)}
body{
  margin:0;
  background:var(--cream);
  color:var(--ink);
  font-family:'Nunito',sans-serif;
  font-size:17px;
  line-height:1.6;
  -webkit-font-smoothing:antialiased;
}
h1,h2,h3,h4{
  font-family:'Baloo 2',cursive;
  color:var(--ink);
  line-height:1.15;
  margin:0 0 .5em;
}
h1{font-size:clamp(2.3rem,6vw,3.6rem); font-weight:800}
h2{font-size:clamp(1.8rem,4vw,2.5rem); font-weight:700}
h3{font-size:1.3rem; font-weight:700}
p{margin:0 0 1em}
a{color:inherit}
img{max-width:100%; display:block}
.container{max-width:var(--maxw); margin-inline:auto; padding-inline:24px}
.eyebrow{
  display:inline-flex; align-items:center; gap:8px;
  font-family:'Baloo 2',cursive; font-weight:600; font-size:.85rem;
  letter-spacing:.06em; text-transform:uppercase;
  color:var(--purple-dark); margin-bottom:14px;
}
.eyebrow::before{content:''; width:9px; height:9px; border-radius:50%; background:var(--yellow); box-shadow:0 0 0 4px rgba(255,217,61,.35)}
.section{padding:88px 0}
.section--alt{background:var(--sand)}
.section-head{max-width:680px; margin-bottom:48px}
.section-head.center{margin-inline:auto; text-align:center}
.visually-hidden{position:absolute; width:1px; height:1px; overflow:hidden; clip:rect(0 0 0 0); white-space:nowrap}

/* Buttons */
.btn{
  display:inline-flex; align-items:center; justify-content:center; gap:8px;
  font-family:'Baloo 2',cursive; font-weight:700; font-size:1rem;
  padding:14px 28px; border-radius:100px; border:2px solid transparent;
  cursor:pointer; text-decoration:none; transition:transform .18s ease, box-shadow .18s ease, background .18s ease;
  white-space:nowrap;
}
.btn:active{transform:translateY(1px)}
.btn-primary{background:var(--purple); color:#fff; box-shadow:0 8px 20px rgba(139,46,147,.35)}
.btn-primary:hover{background:var(--purple-dark); box-shadow:0 10px 24px rgba(139,46,147,.45); transform:translateY(-2px)}
.btn-secondary{background:#fff; color:var(--ink); border-color:var(--line)}
.btn-secondary:hover{border-color:var(--teal); color:var(--teal-dark); transform:translateY(-2px)}
.btn-sun{background:var(--yellow); color:var(--ink); box-shadow:0 8px 20px rgba(245,185,33,.4)}
.btn-sun:hover{background:var(--yellow-deep); transform:translateY(-2px)}
.btn-block{width:100%}

/* Skip link */
.skip-link{
  position:absolute; left:12px; top:-60px; background:var(--ink); color:#fff;
  padding:12px 18px; border-radius:10px; z-index:1000; transition:top .2s ease;
  font-family:'Baloo 2',cursive; font-weight:600; text-decoration:none;
}
.skip-link:focus{top:12px}

:focus-visible{outline:3px solid var(--teal); outline-offset:3px}

/* ============================================================
   HEADER
============================================================ */
header.site-header{
  position:sticky; top:0; z-index:900;
  background:rgba(255,251,242,.9);
  backdrop-filter:blur(10px);
  border-bottom:1px solid transparent;
  transition:border-color .25s ease, box-shadow .25s ease, padding .25s ease;
}
header.site-header.scrolled{border-color:var(--line); box-shadow:0 6px 20px rgba(46,33,64,.06)}
.nav-wrap{
  height:var(--header-h);
  display:flex; align-items:center; justify-content:space-between; gap:16px;
}
.brand{display:flex; align-items:center; gap:12px; text-decoration:none; flex-shrink:0}
.brand img{height:52px; width:52px; object-fit:contain}
.brand-text{font-family:'Baloo 2',cursive; font-weight:800; font-size:1.15rem; color:var(--ink); line-height:1.1}
.brand-text span{display:block; font-family:'Nunito',sans-serif; font-weight:600; font-size:.68rem; letter-spacing:.08em; text-transform:uppercase; color:var(--teal-dark)}

nav.primary-nav{display:flex; align-items:center; gap:6px}
nav.primary-nav a{
  font-family:'Baloo 2',cursive; font-weight:600; font-size:.98rem;
  color:var(--ink-soft); text-decoration:none; padding:10px 14px; border-radius:100px;
  transition:background .18s ease, color .18s ease;
  position:relative;
}
nav.primary-nav a:hover{background:rgba(139,46,147,.08); color:var(--purple)}
nav.primary-nav a.active{color:var(--purple); background:rgba(139,46,147,.1)}

.header-actions{display:flex; align-items:center; gap:12px}
.menu-toggle{
  display:none; align-items:center; justify-content:center;
  width:46px; height:46px; border-radius:14px; border:1px solid var(--line);
  background:#fff; cursor:pointer;
}
.menu-toggle svg{width:22px; height:22px}
.menu-toggle[aria-expanded="true"] .icon-open{display:none}
.menu-toggle[aria-expanded="false"] .icon-close{display:none}

@media (max-width:900px){
  nav.primary-nav{
    position:fixed; inset:var(--header-h) 0 0 0; height:calc(100dvh - var(--header-h));
    background:var(--cream); flex-direction:column; justify-content:flex-start;
    padding:28px 24px; gap:4px; transform:translateX(100%); transition:transform .3s ease;
    overflow-y:auto;
  }
  nav.primary-nav.open{transform:translateX(0)}
  nav.primary-nav a{width:100%; padding:16px 18px; font-size:1.1rem; border-bottom:1px solid var(--line); border-radius:12px}
  .nav-cta{margin-top:16px; width:100%}
  .header-actions .btn-primary.desktop-only{display:none}
  .menu-toggle{display:inline-flex}
}
@media (min-width:901px){
  .nav-cta{display:none}
}

/* ============================================================
   HERO
============================================================ */
.hero{position:relative; overflow:hidden; padding:64px 0 96px}
.hero::before{
  content:''; position:absolute; inset:0; z-index:0;
  background:
    radial-gradient(560px 420px at 82% 8%, rgba(255,217,61,.35), transparent 65%),
    radial-gradient(420px 340px at -5% 90%, rgba(42,175,196,.18), transparent 70%);
}
.hero-grid{position:relative; z-index:1; display:grid; grid-template-columns:1.1fr .9fr; gap:56px; align-items:center}
.hero-copy .pill-row{display:flex; flex-wrap:wrap; gap:10px; margin:26px 0 32px}
.pill{
  display:inline-flex; align-items:center; gap:8px; background:#fff; border:1px solid var(--line);
  padding:9px 16px; border-radius:100px; font-weight:700; font-size:.86rem; color:var(--ink-soft);
  box-shadow:0 4px 10px rgba(46,33,64,.05);
}
.pill span.dot{width:8px; height:8px; border-radius:50%}
.hero-cta-row{display:flex; flex-wrap:wrap; gap:14px}
.hero-copy p.lead{font-size:1.15rem; color:var(--ink-soft); max-width:52ch}

.hero-art{position:relative; display:flex; align-items:center; justify-content:center}


.hero-photo{
  width:100%;
max-width:780px;
  aspect-ratio:4 / 3;
  object-fit:cover;
  object-position:center;
  border-radius:28px;
  box-shadow:0 24px 60px rgba(46,33,64,.18);
  border:6px solid rgba(255,255,255,.75);
  display:block;
}

.sun-badge{width:min(360px,80%); aspect-ratio:1; position:relative}
.sun-badge img{width:100%; height:100%; object-fit:contain; filter:drop-shadow(0 20px 40px rgba(139,46,147,.22))}
.sun-badge .float-chip{
  position:absolute; background:#fff; border-radius:18px; padding:12px 16px; box-shadow:var(--shadow);
  font-family:'Baloo 2',cursive; font-weight:700; font-size:.85rem; display:flex; align-items:center; gap:8px;
  border:1px solid var(--line);
}
.chip-1{top:6%; left:-8%; color:var(--teal-dark)}
.chip-2{bottom:10%; right:-10%; color:var(--pink)}

@keyframes floaty{0%,100%{transform:translateY(0)} 50%{transform:translateY(-10px)}}
.float-chip{animation:floaty 5s ease-in-out infinite}
.chip-2{animation-delay:1.4s}

@media (max-width:960px){
  .hero-grid{grid-template-columns:1fr; text-align:center}
  .hero-copy .pill-row, .hero-cta-row{justify-content:center}
  .hero-copy p.lead{margin-inline:auto}
  .hero-art{order:-1; margin-bottom:8px}
  .sun-badge{width:min(260px,70%)}
  .float-chip{display:none}
}

/* ============================================================
   WAVY DIVIDER
============================================================ */
.divider{display:block; width:100%; height:60px; margin-top:-1px}

/* ============================================================
   SERVICES
============================================================ */
.services-grid{display:grid; grid-template-columns:repeat(3,1fr); gap:24px}
.service-card{
  background:#fff; border-radius:var(--radius); padding:30px 26px; border:1px solid var(--line);
  box-shadow:var(--shadow); position:relative; overflow:hidden;
  transition:transform .25s ease, box-shadow .25s ease;
}
.service-card:hover{transform:translateY(-6px); box-shadow:0 20px 40px rgba(46,33,64,.16)}
.service-card::before{content:''; position:absolute; top:0; left:0; right:0; height:6px; background:var(--accent,var(--teal))}
.service-icon{
  width:54px; height:54px; border-radius:16px; display:flex; align-items:center; justify-content:center;
  background:color-mix(in srgb, var(--accent,var(--teal)) 16%, white); margin-bottom:18px; font-size:1.6rem;
}
.service-card h3{margin-bottom:8px}
.service-card p{color:var(--ink-soft); font-size:.98rem; margin-bottom:0}

@media (max-width:960px){.services-grid{grid-template-columns:1fr 1fr}}
@media (max-width:640px){.services-grid{grid-template-columns:1fr}}

/* ============================================================
   ABOUT
============================================================ */
.about-grid{display:grid; grid-template-columns:.95fr 1.05fr; gap:56px; align-items:center}
.about-art{
  position:relative; background:#fff; border:1px solid var(--line); border-radius:var(--radius);
  padding:36px; box-shadow:var(--shadow);
}
.team-row{display:flex; flex-wrap:wrap; gap:12px; margin-top:22px}
.team-chip{
  display:flex; align-items:center; gap:10px; background:var(--sand); border-radius:100px; padding:8px 16px 8px 8px;
  font-family:'Baloo 2',cursive; font-weight:700; font-size:.92rem;
}
.team-chip .avatar{
  width:34px; height:34px; border-radius:50%; display:flex; align-items:center; justify-content:center;
  color:#fff; font-family:'Baloo 2',cursive; font-weight:800;
}
.value-list{list-style:none; margin:24px 0 0; padding:0; display:grid; gap:14px}
.value-list li{display:flex; gap:12px; align-items:flex-start; font-weight:700; color:var(--ink)}
.value-list li svg{flex-shrink:0; margin-top:3px}
@media (max-width:900px){.about-grid{grid-template-columns:1fr}}

/* ============================================================
   STEAM
============================================================ */
.steam-grid{display:grid; grid-template-columns:repeat(5,1fr); gap:18px}
.steam-card{
  background:#fff; border-radius:18px; padding:22px 16px; text-align:center; border:1px solid var(--line);
  box-shadow:0 8px 20px rgba(46,33,64,.06);
}
.steam-card .letter{
  width:44px; height:44px; border-radius:50%; margin:0 auto 12px; display:flex; align-items:center; justify-content:center;
  font-family:'Baloo 2',cursive; font-weight:800; color:#fff; font-size:1.1rem;
}
.steam-card p{font-size:.9rem; color:var(--ink-soft); margin:0}
@media (max-width:900px){.steam-grid{grid-template-columns:repeat(3,1fr)}}
@media (max-width:560px){.steam-grid{grid-template-columns:repeat(2,1fr)}}

/* ============================================================
   HOW IT WORKS
============================================================ */
.steps{list-style:none; margin:0; padding:0; display:grid; grid-template-columns:repeat(4,1fr); gap:22px; counter-reset:step}
.step{position:relative; padding:34px 22px 22px; background:#fff; border-radius:18px; border:1px solid var(--line)}
.step::before{
  counter-increment:step; content:counter(step);
  position:absolute; top:-20px; left:22px; width:44px; height:44px; border-radius:50%;
  background:var(--purple); color:#fff; font-family:'Baloo 2',cursive; font-weight:800; font-size:1.1rem;
  display:flex; align-items:center; justify-content:center; box-shadow:0 8px 18px rgba(139,46,147,.35);
}
.step h3{font-size:1.08rem; margin-top:6px}
.step p{color:var(--ink-soft); font-size:.94rem; margin-bottom:0}
@media (max-width:900px){.steps{grid-template-columns:1fr 1fr}}
@media (max-width:560px){.steps{grid-template-columns:1fr}}

/* ============================================================
   TESTIMONIALS
============================================================ */
.testi-wrap{position:relative}
.testi-track{
  display:flex; gap:22px; overflow-x:auto; scroll-snap-type:x mandatory; padding:6px 6px 26px;
  scrollbar-width:none;
}
.testi-track::-webkit-scrollbar{display:none}
.testi-card{
  scroll-snap-align:start; flex:0 0 min(420px,86vw); background:#fff; border-radius:var(--radius);
  padding:30px; border:1px solid var(--line); box-shadow:var(--shadow); display:flex; flex-direction:column;
}
.testi-quote{font-size:1.32rem; line-height:1.15; font-family:'Baloo 2',cursive; color:var(--purple); margin-bottom:14px}
.testi-card p.text{color:var(--ink-soft); flex:1}
.testi-name{font-family:'Baloo 2',cursive; font-weight:700; margin-top:14px; color:var(--ink)}
.testi-controls{display:flex; justify-content:center; gap:12px; margin-top:6px}
.testi-controls button{
  width:44px; height:44px; border-radius:50%; border:1px solid var(--line); background:#fff; cursor:pointer;
  display:flex; align-items:center; justify-content:center; transition:background .18s ease;
}
.testi-controls button:hover{background:var(--sand)}

/* ============================================================
   FAQ
============================================================ */
.faq-list{display:grid; gap:14px; max-width:820px}
.faq-item{background:#fff; border:1px solid var(--line); border-radius:16px; overflow:hidden}
.faq-item summary{
  cursor:pointer; list-style:none; padding:20px 24px; font-family:'Baloo 2',cursive; font-weight:700;
  display:flex; align-items:center; justify-content:space-between; gap:16px;
}
.faq-item summary::-webkit-details-marker{display:none}
.faq-item summary .plus{
  flex-shrink:0; width:28px; height:28px; border-radius:50%; background:var(--sand);
  display:flex; align-items:center; justify-content:center; transition:transform .25s ease;
  font-size:1.2rem; color:var(--purple); font-weight:800;
}
.faq-item[open] summary .plus{transform:rotate(45deg)}
.faq-item .faq-body{padding:0 24px 22px; color:var(--ink-soft)}

/* ============================================================
   CONTACT
============================================================ */
.contact-grid{display:grid; grid-template-columns:1fr 1fr; gap:44px; align-items:start}
.contact-card{background:#fff; border-radius:var(--radius); border:1px solid var(--line); box-shadow:var(--shadow); padding:34px}
.form-row{display:grid; grid-template-columns:1fr 1fr; gap:16px}
.field{display:flex; flex-direction:column; gap:6px; margin-bottom:16px}
.field label{font-weight:700; font-size:.92rem}
.field input, .field select, .field textarea{
  font:inherit; padding:12px 14px; border-radius:12px; border:1.5px solid var(--line); background:var(--cream);
  transition:border-color .18s ease, box-shadow .18s ease;
}
.field input:focus, .field select:focus, .field textarea:focus{
  border-color:var(--teal); box-shadow:0 0 0 4px rgba(42,175,196,.15); outline:none;
}
.field textarea{resize:vertical; min-height:110px}
.contact-info-list{list-style:none; margin:0 0 28px; padding:0; display:grid; gap:18px}
.contact-info-list li{display:flex; gap:14px; align-items:flex-start}
.contact-info-list .ico{
  width:46px; height:46px; border-radius:14px; background:var(--sand); flex-shrink:0;
  display:flex; align-items:center; justify-content:center;
}
.contact-info-list strong{display:block; font-family:'Baloo 2',cursive}
.social-row{display:flex; gap:12px; margin-top:8px}
.social-row a{
  width:46px; height:46px; border-radius:50%; background:var(--ink); color:#fff; display:flex;
  align-items:center; justify-content:center; transition:background .18s ease, transform .18s ease;
}
.social-row a:hover{background:var(--purple); transform:translateY(-3px)}
@media (max-width:900px){.contact-grid{grid-template-columns:1fr}}
@media (max-width:560px){.form-row{grid-template-columns:1fr}}

/* dialog */
dialog#confirmDialog{
  border:none; border-radius:24px; padding:0; max-width:420px; width:90vw; box-shadow:0 30px 60px rgba(46,33,64,.35);
}
dialog#confirmDialog::backdrop{background:rgba(46,33,64,.55); backdrop-filter:blur(2px)}
.dialog-inner{padding:36px 30px; text-align:center}
.dialog-inner .sun-mini{width:64px; height:64px; margin:0 auto 16px}
.dialog-inner h3{margin-bottom:10px}
.dialog-inner p{color:var(--ink-soft)}

/* ============================================================
   CTA BAND
============================================================ */
.cta-band{
  background:linear-gradient(120deg,var(--purple),var(--wine));
  border-radius:32px; padding:56px 40px; text-align:center; color:#fff; position:relative; overflow:hidden;
}
.cta-band h2{color:#fff}
.cta-band p{color:rgba(255,255,255,.88); max-width:56ch; margin-inline:auto}
.cta-band .btn-sun{margin-top:22px}

/* ============================================================
   FOOTER
============================================================ */
footer.site-footer{background:var(--ink); color:rgba(255,255,255,.78); padding:56px 0 26px}
.footer-grid{display:grid; grid-template-columns:1.4fr 1fr 1fr 1fr; gap:36px}
.footer-grid h4{color:#fff; font-family:'Baloo 2',cursive; margin-bottom:14px; font-size:1.02rem}
.footer-brand{display:flex; align-items:center; gap:12px; margin-bottom:14px}
.footer-brand img{height:44px; width:44px}
.footer-brand span{font-family:'Baloo 2',cursive; color:#fff; font-weight:800}
footer.site-footer ul{list-style:none; margin:0; padding:0; display:grid; gap:10px}
footer.site-footer a{color:rgba(255,255,255,.78); text-decoration:none; font-size:.95rem}
footer.site-footer a:hover{color:var(--yellow)}
.footer-bottom{
  border-top:1px solid rgba(255,255,255,.14); margin-top:40px; padding-top:22px;
  display:flex; flex-wrap:wrap; gap:12px; justify-content:space-between; font-size:.85rem;
}
@media (max-width:860px){.footer-grid{grid-template-columns:1fr 1fr}}
@media (max-width:520px){.footer-grid{grid-template-columns:1fr}}

/* Back to top */
#backToTop{
  position:fixed; right:22px; bottom:22px; z-index:500; width:52px; height:52px; border-radius:50%;
  background:var(--yellow); border:none; box-shadow:0 10px 24px rgba(245,185,33,.5); cursor:pointer;
  display:flex; align-items:center; justify-content:center; opacity:0; pointer-events:none;
  transform:translateY(10px); transition:opacity .25s ease, transform .25s ease;
}
#backToTop.show{opacity:1; pointer-events:auto; transform:translateY(0)}

/* reveal animation */
.reveal{opacity:0; transform:translateY(24px); transition:opacity .6s ease, transform .6s ease}
.reveal.in-view{opacity:1; transform:translateY(0)}

@media (prefers-reduced-motion: reduce){
  html{scroll-behavior:auto}
  .reveal{opacity:1; transform:none; transition:none}
  .float-chip{animation:none}
  *{scroll-behavior:auto !important}
}

/* ============================================================
   TRUST BAR (BBB)
============================================================ */
.trust-bar{background:#fff; border-bottom:1px solid var(--line)}
.trust-bar-inner{
  display:flex; align-items:center; justify-content:center; gap:16px;
  padding:12px 24px; flex-wrap:wrap; text-align:center;
}
.trust-bar-inner p{margin:0; font-size:.92rem; color:var(--ink-soft)}
.bbb-link{display:inline-flex; flex-shrink:0; border-radius:8px; transition:transform .18s ease, box-shadow .18s ease}
.bbb-link:hover{transform:translateY(-2px)}
.bbb-link img{height:40px; width:auto; display:block}

/* ============================================================
   VIDEO CARD (About)
============================================================ */
.video-card{
  position:relative; background:#fff; border:1px solid var(--line); border-radius:var(--radius);
  box-shadow:var(--shadow); padding:14px; overflow:visible;
}
.video-card .video-frame{
  position:relative; width:100%; aspect-ratio:16/9; border-radius:16px; overflow:hidden;
  background:var(--ink); box-shadow:inset 0 0 0 1px rgba(255,255,255,.06);
}
.video-card .video-frame iframe{position:absolute; inset:0; width:100%; height:100%; border:0}
.video-badge{
  position:absolute; top:-16px; left:24px; background:var(--yellow); color:var(--ink);
  font-family:'Baloo 2',cursive; font-weight:800; font-size:.85rem; padding:9px 18px;
  border-radius:100px; box-shadow:0 8px 18px rgba(245,185,33,.45); display:inline-flex; align-items:center; gap:6px;
  z-index:2; transform:rotate(-2deg);
}
.video-caption{display:flex; align-items:center; gap:12px; padding:16px 8px 6px}
.video-caption img{
  width:48px; height:48px; border-radius:50%; object-fit:cover; border:2px solid #fff; box-shadow:0 4px 10px rgba(46,33,64,.18);
  flex-shrink:0;
}
.video-caption strong{font-family:'Baloo 2',cursive; display:block; font-size:.95rem}
.video-caption span{color:var(--ink-soft); font-size:.85rem}

/* ============================================================
   PARTNERS / BUSINESSES WE SUPPORT
============================================================ */
.partners-grid{display:grid; grid-template-columns:repeat(4,1fr); gap:20px}
.partner-card{
  background:#fff; border:1px solid var(--line); border-radius:18px; padding:22px;
  display:flex; flex-direction:column; align-items:center; text-align:center; gap:12px;
  text-decoration:none; color:var(--ink); box-shadow:0 6px 16px rgba(46,33,64,.05);
  transition:transform .2s ease, box-shadow .2s ease, border-color .2s ease;
}
.partner-card:hover{transform:translateY(-4px); box-shadow:0 14px 28px rgba(46,33,64,.12); border-color:var(--teal)}
.partner-card img{width:100%; max-width:120px; height:64px; object-fit:contain}
.partner-card span{font-family:'Baloo 2',cursive; font-weight:700; font-size:.92rem}
.partner-card.placeholder{
  border-style:dashed; border-width:2px; color:var(--ink-soft); cursor:default;
}
.partner-card.placeholder:hover{transform:none; box-shadow:0 6px 16px rgba(46,33,64,.05); border-color:var(--line)}
.partner-card.placeholder .placeholder-icon{
  width:64px; height:64px; border-radius:16px; background:var(--sand); display:flex; align-items:center; justify-content:center;
  color:var(--purple); font-size:1.6rem;
}
@media (max-width:960px){.partners-grid{grid-template-columns:repeat(2,1fr)}}
@media (max-width:560px){.partners-grid{grid-template-columns:1fr}}

/* ============================================================
   PRICING (Subscriptions page)
============================================================ */
.pricing-grid{display:grid; grid-template-columns:repeat(3,1fr); gap:24px}
.price-card{
  background:#fff; border-radius:var(--radius); padding:30px 26px; border:1px solid var(--line);
  box-shadow:var(--shadow); display:flex; flex-direction:column; position:relative; overflow:hidden;
}
.price-card::before{content:''; position:absolute; top:0; left:0; right:0; height:6px; background:var(--accent,var(--teal))}
.price-card h3{margin-bottom:4px}
.price-card .price{font-family:'Baloo 2',cursive; font-weight:800; font-size:2.1rem; color:var(--ink); margin:6px 0 2px}
.price-card .price small{font-family:'Nunito',sans-serif; font-weight:700; font-size:.9rem; color:var(--ink-soft)}
.price-card .reg-fee{font-size:.82rem; color:var(--purple-dark); font-weight:700; margin-bottom:14px}
.price-card p.desc{color:var(--ink-soft); font-size:.94rem}
.price-card ul{list-style:none; margin:14px 0 24px; padding:0; display:grid; gap:10px; flex:1}
.price-card ul li{display:flex; gap:10px; align-items:flex-start; font-size:.92rem; color:var(--ink)}
.price-card ul li svg{flex-shrink:0; margin-top:3px}
.price-card .btn{margin-top:auto}
.price-note{
  background:#fff; border:1px solid var(--line); border-radius:var(--radius); padding:26px 30px;
  box-shadow:var(--shadow); margin-top:8px;
}
/* Full-width homeschool card */
.homeschool-card{
  grid-column:1 / -1;

}
.homeschool-content{
  display:grid;
  grid-template-columns:1.5fr 1fr;
  gap:40px;
  align-items:center;
}
.homeschool-content .btn{
  width: auto;
  justify-self: start;
  padding-left: 32px;
  padding-right: 32px;
}
.homeschool-stepup{
  padding:28px;
  background:var(--sand);
  border-radius:var(--radius);
}

.stepup-logo{
    display:block;
    width:180px;
    max-width:100%;
    height:auto;
    margin:0 auto 16px;
}

@media (max-width:760px){
  .homeschool-content{
    grid-template-columns:1fr;
  }
}

@media (max-width:960px){.pricing-grid{grid-template-columns:1fr 1fr}}
@media (max-width:640px){.pricing-grid{grid-template-columns:1fr}}

/* ============================================================
   ABOUT PAGE
============================================================ */
.founder-hero{display:grid; grid-template-columns:.8fr 1.2fr; gap:56px; align-items:center}
.founder-hero img{border-radius:var(--radius); box-shadow:var(--shadow); width:100%}
.credentials-list{list-style:none; margin:20px 0 0; padding:0; display:grid; gap:12px}
.credentials-list li{display:flex; gap:12px; align-items:flex-start; font-weight:700}
.credentials-list li svg{flex-shrink:0; margin-top:3px}
.timeline{display:grid; gap:22px; max-width:760px; margin-inline:auto}
.timeline-item{background:#fff; border:1px solid var(--line); border-radius:18px; padding:24px 26px; box-shadow:0 8px 20px rgba(46,33,64,.06)}
.timeline-item h3{font-size:1.05rem; margin-bottom:6px; color:var(--purple-dark)}
.timeline-item p{margin-bottom:0; color:var(--ink-soft); font-size:.96rem}
.signature-block{margin-top:28px; font-family:'Baloo 2',cursive; font-size:1.15rem; color:var(--purple-dark)}
@media (max-width:900px){.founder-hero{grid-template-columns:1fr}}
